host_groups:
  sal-1:
    host_vars:
      sap:
        sid: PRD
        instance_nr: "00"
    hosts:
      dos.salconsultancy.nl:
        host_template: sap_app_server
        host_checks:
          - check_sap_instance
