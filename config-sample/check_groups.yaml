check_groups:
  sap_os:
    vars:
      sap:
        instance_nr: "00"
    checks:
      - check_uptime
  sap_app:
    vars:
      sap:
        profile_path: /usr/sap/${sap.sid}/SYS/profile
    checks:
      - check_sap_profile_dir
